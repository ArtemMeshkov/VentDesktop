<UserControl x:Class="BMC.View.PlateBypassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BMC.View"
             mc:Ignorable="d" 
             d:DesignHeight="466" d:DesignWidth="240" >
    <UserControl.Resources>
       
    </UserControl.Resources>
    <Grid>
        <WrapPanel Margin="10,20">
            <Label Content="Питание привода, В " Margin="0,0,10,0" FontSize="14"/>
            <ComboBox  ItemsSource="{Binding Power}" SelectedItem="{Binding SelectedPower}" MinWidth="70"/>
        </WrapPanel>
            
    </Grid>
</UserControl>
